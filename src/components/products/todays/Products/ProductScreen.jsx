import React from 'react'
// import data from '../../../../datas/data'

const ProductScreen = ({ match }) => {

  // const product = data.find((p) => p._id === match.params.id)

  return (
    <div>
      {/* {product.name} */}
      product
    </div>
  )
}

export default ProductScreen
